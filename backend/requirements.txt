# --- Servicio FastAPI + modelo (runtime de /predictions y /admin/recompute) ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27          # cliente HTTP para football-data.org (la ingesta del día)
slowapi>=0.1.9       # rate limit por IP en /predictions (anti-abuso)
numpy>=1.26
scipy>=1.11          # distribución de Poisson (marcadores)

# --- Fase 2: modelo entrenado (regresión de Poisson) ---
pandas>=2.1
statsmodels>=0.14    # GLM familia Poisson

# --- Desarrollo / test ---
pytest>=8.0
